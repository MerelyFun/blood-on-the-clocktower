# npm run dev 默认使用本机 SQLite，无需填写以下 Supabase 配置。
# 开发切回云端时取消下一行注释；生产构建默认使用云端。
# VITE_BACKEND=supabase
# 不要将 VITE_BACKEND=local 的构建部署到 Pages；它需要同时运行本机 API。
# 本地启动参数通过终端环境变量覆盖：
# CLOCKTOWER_PORT=5174
# CLOCKTOWER_API_PORT=5175
# LOCAL_DATABASE_PATH=.local-data/clocktower.sqlite

# 仅填写前端公开配置。不要填写 secret / service_role 密钥。
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
# 项目站点由 Actions 自动设置，也可留空使用相对路径。
VITE_BASE_PATH=
# 可选：为匿名登录启用 Cloudflare Turnstile 时填写
VITE_TURNSTILE_SITE_KEY=
