# 血染钟楼网站 · 分析与使用说明

> **2026-09-10 修复进展：** 以下正文保留为原始静态审阅记录。当前源码已修复旅行者普通提名/处决限制、自定义旅行者入口、存档深层校验、云端创建/恢复重试、局部夜序覆盖、跨阶段计票与备份大小限制。已通过 34 项自动测试、前端和 Edge Function 类型检查、本机桌面与手机宽度浏览器流程。Supabase 数据库与函数已部署，实际云端创建、审批、发牌、私信和跨账号权限隔离、重试与换绑及双浏览器测试通过；dist 已带公开连接配置重新构建。个人邮箱登录、真实手机、长期断线重连和前端公网发布仍待完成；配置状态见 [Supabase 配置清单](docs/SUPABASE_SETUP.md)。成员审批失败会刷新并提示核对状态，尚未加入服务端操作 ID 去重；大规模界面格式整理和云端归档删除仍为后续工作。

*SOURCE REVIEW  /  VERSION 1.0.0  /  2026.09.10*

> 结论：这是一套已有主要业务代码的“说书人辅助工具”，不是只有页面的演示稿；但它仍是未完成联机验收的源码交付包，不能直接等同于已上线、已验证可稳定开局的产品。

本说明基于你上传的《血染钟楼网站_源码与部署包_v1(1).zip》。重点回答：它做了什么、怎样使用、为什么需要 Supabase、如何部署，以及继续开发时应先处理哪些问题。

| 项目 | 核实结果 |
| --- | --- |
| 交付组成 | 56 个文件；含前端源码、共享游戏引擎、数据库 SQL、Edge Function、部署工作流、说明文档及 dist 静态产物。 |
| 产品定位 | 真人说书人的配板、发身份、魔典记录和流程辅助；不是自动裁判，也不是内置语音的视频桌游平台。 |
| 运行方式 | 本机主持：数据留在当前浏览器；联机房间：通过 Supabase 让不同设备领取各自身份。 |
| 内置内容 | 3 套基础剧本，共 72 个普通角色；另有 15 个旅行者、8 个传奇、2 个奇遇，共 97 个目录条目。 |
| 交付状态 | 包内构建记录注明：没有运行测试或浏览器检查，没有应用数据库、部署函数或发布 GitHub 站点。 |

### 建议怎样看这份说明

使用者先读第 2—4 页，了解功能与开局方式；负责部署的人读第 7—8 页；继续开发或准备公开使用的人重点读第 9—12 页。最后两页列出证据索引，可按文件和行号复核。

### 本次分析的边界

本次执行了压缩包解包、源码静态审阅、结构统计和相关官方文档核对。没有运行该网站、安装依赖、执行构建或业务测试，也没有连接或改动你的 GitHub、Supabase 项目。文中“已实现”表示代码中存在对应逻辑，不代表已通过实际运行验收。

**来源／范围：** 依据：[S01] 交付与构建记录；[S02] 角色目录。原 ZIP SHA-256：4f5940ba2e10c2f82b53202c5a4e6b2f7fc8c98cadaa987775a72007c51df347。


---


## 01  功能覆盖与完成边界

*先看实际代码覆盖了什么，而不是只看功能名称。*

| 模块 | 代码中已有的能力 | 使用边界 |
| --- | --- | --- |
| 剧本与角色 | 选择三套基础剧本；复制修改角色池；添加自定义角色；导入、导出 JSON。 | 不是全量实验角色资料库；未知 ID 需补全类别、能力与夜序。 |
| 本局配板 | 5—15 名普通玩家；自动按类别配角色或逐座位指定；设置显示身份、阵营和恶魔伪装。 | 角色改变初始人数配比等复杂规则，仍须人工核对。 |
| 发放身份 | 随机或固定分配；发牌后进入首夜；身份版本和已查看确认。 | 联机玩家需批准入座；刷新不应重新抽签。 |
| 邀请与座位 | 6 位房间码、二维码、加入申请、审批、锁房、撤销、换设备重新绑定。 | 匿名身份依赖原浏览器会话；不是昵称登录系统。 |
| 说书人魔典 | 座位、真实与公开生死、真实与显示角色／阵营、死者票、提醒标记、私密笔记。 | 角色能力不会根据标记自动执行；由说书人裁定。 |
| 夜晚工作台 | 首夜／后续夜晚任务；任务完成、目标、备注、手动补任务；私密信息与选择请求。 | 死者可能保留夜间任务；是否唤醒、信息真假需人工判断。 |
| 白天与计票 | 日夜推进、计时、公告；普通提名、记录投票、死者票消耗、处决与死亡分开、旅行者放逐。 | 由说书人录入投票，不是玩家手机实时举手投票；有边界待修复。 |
| 备份与复盘 | 完整存档、公开版本导出；阶段快照与手动快照；导入为新副本；结束与公开复盘。 | 最近 8 份快照不是无限历史；完整存档包含秘密，不能随意分享。 |
| 联机与权限 | 用户认证、成员绑定、数据库权限、玩家专属数据、实时刷新信号、轮询和命令重试。 | 配置与权限脚本尚未在实际项目验证；不能仅凭代码宣称安全验收完成。 |

### 对你原始需求的判断

“选版型并增删角色”“给玩家发身份”“说书人记录与主持”三条主线均有对应实现。现阶段的关键工作不是继续堆功能，而是把已有主线跑通、验证信息隔离，并修复会干扰开局或恢复的边界。

**来源／范围：** 依据：[S02]—[S09]；原需求与范围说明见 [S13]。详细问题见第 9—10 页。


---


## 02  一局游戏应该怎样使用

*推荐操作顺序：先确定角色池与座位，再发身份，最后按日夜推进。*

### 1. 选择剧本，整理角色池

在首页选“暗流涌动”“黯月初升”或“梦殒春宵”；需要增删角色时先复制为自定义剧本。角色池表示“本局可能出现的角色”，不表示每个角色都必须入场。导入后先处理未补全角色和配置警告。发牌后角色池冻结，常规增删应在配板阶段完成；旅行者后加入有单独入口。

### 2. 创建对局，确定本机或联机

单设备主持选“本机主持”；玩家各用手机领取身份选“联机房间”。联机说书人先通过邮箱链接或已配置的 GitHub 登录，再创建房间。普通玩家人数可选 5—15，包含旅行者的总座位上限为 20。

### 3. 邀请玩家并确认入座

联机时分享房间码或二维码。玩家填写昵称申请加入，说书人在“邀请／入座”里批准并绑定座位。建议发牌前完成所有普通玩家审批：发牌操作会自动锁房，之后新增申请需要先解锁。

### 4. 配置本局实际身份

自动配置可以按类别抽选角色，也可逐座位指定。角色导致的特殊配比需要手动调整。必须区分真实身份与展示身份：例如手工配置酒鬼时，还需正确设置其看到的镇民身份，而不是直接显示“酒鬼”。

### 5. 确认随机方式，再发身份

保留随机选项会打乱普通玩家的身份组合；需要指定“某人拿某角色”时，应关闭随机发放。检查显示阵营、恶魔伪装及配置警告后确认。系统进入第 1 夜；玩家揭示自己的身份并确认已查看，重复刷新不会调用重新发牌。

### 6. 按夜序处理信息与状态

按任务记录唤醒、行动目标和结果，使用私密消息或选择请求与玩家交互。夜序提供提醒，不自动处理醉毒、免死、阵营变化或真假信息。真实死亡可先由说书人记录，再在黎明按需公开。

### 7. 白天提名、计票、裁定

进入白天后，由说书人按座位顺序记录投票。确认计票时才消耗计入的死者票；票数决定候选，但处决及是否死亡还需分别确认。旅行者应走单独放逐流程；特殊票数、额外提名和处决须写明裁定原因。

### 8. 导出与结束

关键节点导出完整备份；结束前先决定公开范围，再分享公开版本或开放复盘。恢复会建立新副本而非覆盖原局，联机副本要重新核对玩家与座位，不能认为原房间绑定会自动迁移。

**来源／范围：** 依据：[S03] 配板、发牌与引擎；[S05] 入座；[S06] 夜间信息；[S07] 投票；[S09] 归档。


---


## 03  本机与联机：不要混为一谈

*本机模式是一位说书人的工作台；联机模式才是多设备身份与状态共享。*

| 比较项 | 本机主持 | 联机房间 |
| --- | --- | --- |
| 数据放在哪里 | 当前浏览器 IndexedDB；自定义剧本与设置使用本地存储。 | Supabase 数据库保存房间；浏览器保留登录会话与少量重试状态。 |
| 谁能看到身份 | 完整局面就在说书人浏览器，可轮流展示身份。 | 说书人看完整魔典；玩家获取公开局面和自己的身份、消息。 |
| 换设备 | 不能自动同步，需要导出、导入副本。 | 说书人重新登录；匿名玩家通常需要说书人重新绑定座位。 |
| 是否要数据库 | 不需要 Supabase 才能建立本机对局。 | 需要 Supabase Auth、数据库及 game 函数共同可用。 |
| 是否支持断网操作 | 本机记录不依赖云端；网页和运行资源须先能正常打开。 | 云端变更在离线时拒绝提交，不是离线写入后自动合并。 |

### 最快查看包内静态版本

先安装满足 package.json 要求的 Node.js（≥22.12；包内 Pages 工作流选用 Node 24）。在解压后的 clocktower 目录中运行：

```text
cd clocktower
node scripts/serve-dist.mjs
```

再用同一台电脑浏览器打开 http://localhost:5173。这个方式使用已有 dist，不需要先 npm install；它不会验证当前源码能重新构建，也不会自动部署联机后端。

> serve-dist.mjs 只监听 127.0.0.1。不能把它理解成“启动后手机访问电脑 IP，就拥有完整局域网联机服务”。直接双击 dist/index.html 也不是包内推荐的使用方式。

### 从源码启动或重新构建

```text
npm ci
npm run dev

# 需要生成新的静态产物时执行
npm run build
```

以上是包内约定的命令，本次没有执行。依赖版本写死且带 lockfile，有利于保持版本一致，但其可安装性、编译结果和运行兼容性仍应在目标环境验证。

**来源／范围：** 依据：[S01] package.json；[S04] storage.ts；[S12] serve-dist.mjs、README。匿名会话行为参考 [W02]。


---


## 04  技术架构与关键数据

*前端负责交互；共享引擎负责状态变化；联机数据库负责最终保存与权限。*

| 层次 | 代码采用的方案 | 它的作用 |
| --- | --- | --- |
| 界面 | React 19.2.8 + TypeScript 7.0.2 | 剧本编辑、说书人工作台、玩家身份页。版本为 package.json 声明值。 |
| 构建与托管 | Vite 8.2.2 + GitHub Pages | 把前端构建为静态文件；页面使用 Hash 路由。 |
| 共享游戏逻辑 | supabase/functions/_shared/ | 角色目录、类型、剧本解析、游戏命令；本机与云端复用。 |
| 云端入口 | Supabase Edge Function：game | 验证登录用户与成员身份，受理创建、加入、查询和游戏命令。 |
| 持久化 | PostgreSQL + RLS + SQL RPC | 保存完整状态和不同受众视图，通过事务控制版本与重复操作。 |
| 同步 | bt_signals + 可见页面 10 秒轮询 | 告诉客户端刷新，再重新读取其有权获取的数据。 |

### 一次联机操作的数据路径

```text
说书人点击 → game 验证用户及房间权限 → 共享引擎生成新状态
         → 数据库事务检查版本并写入 → 发出刷新信号
         → 各客户端读取自己的视图
```

本机模式则在浏览器内执行同一引擎，通过 IndexedDB 事务保存，并通知同源标签页刷新。GitHub Pages 不负责计算秘密身份或写数据库。

### 为什么同一个玩家需要多组状态

| 字段／概念 | 说明 |
| --- | --- |
| script.roles 与 seat.roleId | 前者是可用角色池；后者是该座位实际身份。不是一个层次。 |
| roleId / shownRoleId | 真实角色与玩家看到的角色分开，例如酒鬼。 |
| alignment / shownAlignment | 真实阵营与告知阵营分开，不能由角色颜色直接推断。 |
| alive / publicAlive | 真实生死与公开生死分开，便于夜间隐藏死亡及特殊状态。 |
| voteAvailable / cardVersion | 分别记录死者票是否可用、展示身份版本；确认已查看另有 acknowledged。 |
| bluffs | 恶魔伪装是独立列表，不等于所有未入场角色，也不会自动向恶魔发送。 |

**来源／范围：** 依据：[S01] 依赖；[S03] types.ts、engine.ts；[S04] useRoom.ts；[S10] API；[S11] SQL。


---


## 05  权限、隐私与同步机制

*设计方向正确，但必须通过真实项目验证，不能把“写了权限代码”当成“权限已验收”。*

### 谁能读到什么

| 身份 | 设计中的可见范围 | 限制 |
| --- | --- | --- |
| 说书人 | 完整魔典、真实身份、标记、说书人笔记、任务、消息及成员信息。 | 必须是房间主持人；不能仅凭前端传入 host 就获得权限。 |
| 已批准玩家 | 公开局面、自己座位的展示身份和私密消息。 | 不能读取其他玩家专属视图或完整 bt_games。 |
| 待批准用户 | 等待入座所需的有限房间信息。 | 未批准前不发身份；加入房间码本身不等于获得座位。 |
| 撤销／旧设备 | 后续请求重新校验当前绑定。 | 已看到的信息、截图和客户端曾收到的数据无法追溯收回。 |

数据库共有 9 张 public 业务表和 1 张私有限流表。完整状态 bt_games、公开状态 bt_public、个人状态 bt_player_views 分开保存；bt_signals 只发送刷新信号。各表有 RLS 策略及显式授权，关键游戏写入只经服务端事务函数。

### verify_jwt=false 并不等于接口免登录

函数配置关闭了网关默认 JWT 校验，但 game 内部要求 Bearer token，并调用 auth.getUser(token) 取得真实用户，再核对成员关系。这里不能仅凭配置项就判定“任何人都能当说书人”；真正应验收的是无 token、伪造角色和跨房间访问是否均被拒绝。[W01]

### 并发与网络超时怎样处理

游戏命令带操作 ID，主持人命令还带预期版本。数据库检查重复命令及版本，避免同一操作重复扣票或把旧局面覆盖回去。命令超时后，前端保留原请求，可“安全重试上次提交”；刷新和重试不应重新抽身份。

不过，创建房间、导入副本及成员审批并未统一接入这套待确认请求机制。不能把游戏命令的幂等保障扩写成“所有按钮都能任意重复提交”。详见第 10 页。

### 玩家页的防误看，不是防作弊承诺

玩家揭示身份后，切后台、失去焦点或约 30 秒会隐藏身份展示。这能减少误看，但不防截图或开发者工具。前端只能放公开 publishable／anon 配置；service_role、secret key 和部署 Access Token 不可写入 VITE_*。

**来源／范围：** 依据：[S04] 同步重试；[S05] 玩家页；[S10] game/index.ts:14—38、61—76；[S11] schema.sql:35—94、109—164；官方 [W01]—[W03]。


---


## 06  联机准备：Supabase 部分

*网站页面、数据库、认证、后端函数缺一不可。以下是操作说明，不代表本次已经执行。*

### 第一步：初始化数据库

选定专用 Supabase 项目，在 SQL Editor 执行 supabase/schema.sql 全文。它是首次安装脚本，不是可反复执行的重置脚本。已有同名表时不要靠删表处理报错；后续变化应使用迁移。包内也提供 CLI 生成初始迁移的辅助脚本，二选一安装即可。

### 第二步：配置登录方式

启用 Anonymous Sign-ins 供玩家加入。说书人可使用邮箱登录链接；使用 GitHub 登录还需启用对应 Provider。Site URL 和 Redirect URLs 填最终网站完整地址，包含项目站点路径；开发时另加 localhost 地址。

玩家清除站点数据、退出匿名账号或更换设备后，原匿名身份可能无法自行恢复，需要重新申请并由说书人替换座位绑定。若启用 Turnstile，要同时配置 Supabase 端 secret 和网站公开 site key。[W02]

### 第三步：配置允许来源并部署 game

按 Supabase 官方安装说明准备 CLI，先查看帮助，再登录和执行包内约定流程：

```text
supabase --help
supabase login
supabase link --project-ref YOUR_PROJECT_REF
supabase secrets set ALLOWED_ORIGINS=https://YOUR_NAME.github.io,http://localhost:5173
supabase functions deploy game --project-ref YOUR_PROJECT_REF
```

| 容易混淆的配置 | 正确含义／示例 |
| --- | --- |
| Site URL / Redirect URLs | 完整站点地址，如 https://YOUR_NAME.github.io/clocktower/。 |
| ALLOWED_ORIGINS | 协议 + 域名 + 必要端口；如 https://YOUR_NAME.github.io，不加 /clocktower/ 或末尾 /。 |
| GitHub OAuth 回调 | 填写 Supabase 提供的 /auth/v1/callback 地址，不填写 GitHub Pages 页面地址。 |
| 服务端管理密钥 | game 读取托管环境中的 SUPABASE_SERVICE_ROLE_KEY；绝不能搬到前端构建变量。 |

> 只把 Supabase URL 和 key 填进网页，并不会自动创建表、部署函数、启用匿名登录或配置 CORS。前端“已配置”不等于整个服务已就绪。

**来源／范围：** 依据：[S10] 函数与 config.toml；[S11] 数据库；[S12] docs/DEPLOYMENT.md:5—73。官方核对见 [W01]—[W03]。


---


## 07  发布配置：GitHub Pages 部分

*已有工作流，但不是“一次上传压缩包，所有服务自动上线”。*

### 第四步：放对仓库目录并配置公开变量

把解压后 clocktower 目录里面的内容放到仓库根目录，保留隐藏的 .github 文件夹。不要让 package.json 再套在一层 clocktower/ 下；现有工作流按根目录安装和构建。

| GitHub Actions 设置位置 | 名称 | 用途 |
| --- | --- | --- |
| Repository variable | VITE_SUPABASE_URL | 联机必填，Supabase 项目 URL。 |
| Repository variable | VITE_SUPABASE_PUBLISHABLE_KEY | 联机必填，前端公开 publishable key；代码兼容旧 anon key。 |
| Repository variable | VITE_TURNSTILE_SITE_KEY | 按验证码设置填写的公开 site key。 |
| Repository variable | SUPABASE_PROJECT_ID | 仅使用后端部署工作流时需要，填项目 ref。 |
| Repository secret | SUPABASE_ACCESS_TOKEN | 仅使用后端部署工作流时需要，属于管理凭据，不是前端 key。 |

### 第五步：分别理解两个工作流

“Deploy website to GitHub Pages”在 main 分支提交或手动触发时，安装依赖、构建 dist 并发布。仓库 Settings → Pages 的 Source 应选 GitHub Actions；工作流会设置项目 base 路径，页面路由使用 #/cloud/… 等形式。[W04]

“Deploy Supabase game function”仅手动触发，只部署 game 函数。它不会应用 schema.sql，不会替你设置认证回调、匿名登录或 ALLOWED_ORIGINS。两个工作流都没有完整业务验收步骤。

### 网页设置为什么不能替代正式发布配置

> “设置”页面保存的是当前浏览器的 localStorage。说书人填好后，其他玩家的手机不会自动获得这份配置。面向多人使用，应在构建前写好公开 VITE_* 变量，再重新发布。

包内 dist 标记为未配置云端。把它直接托管只能得到未统一配置的静态前端。旧浏览器中保存的设置还会优先于新的构建变量，切换 Supabase 项目后需检查是否仍沿用旧设置。

### 上线前必须人工确认

网站可打开、登录能回到正确页面、game 能访问数据库、玩家申请与审批成功、不同账号看不到彼此秘密。不要提交完整魔典导出、身份表、个人 .env 或服务端密钥。

**来源／范围：** 依据：[S01] BUILD_INFO.json；[S04] supabase.ts:3—11；[S12] 两个工作流及 DEPLOYMENT.md。官方 Pages 工作流要求见 [W04]。


---


## 08  静态审阅发现：优先处理

*以下是代码路径可确认的问题或缺口；影响描述是静态推断，尚未通过运行复现。*

### P1-01  普通提名没有排除旅行者

证据：Votes.tsx:7 在普通提名时展示所有未离场座位；engine.ts:111 只检查“放逐目标必须是旅行者”，没有反向限制普通提名目标。后续常规处决也没有补这个检查。

影响：旅行者可以沿普通提名进入常规处决路径。官方规则区分普通处决与旅行者放逐；角色能力造成的特殊处决另当别论。[W05] 建议前端与引擎同时约束普通提名目标，并为明确的能力例外保留带原因的独立人工裁定，而不是让常规流程默认放行。

### P1-02  导入了自定义旅行者，常用选择入口却找不到

证据：Setup.tsx:10—11、SeatInspector.tsx:11 的旅行者选项来自内置 CATALOG；引擎 add_traveller 则能够查找当前剧本里的自定义角色。因此数据层可表达，不代表操作界面真正接通。

影响：导入自定义旅行者后，用户仍可能无法通过常用界面添加或切换该角色。建议统一使用“当前剧本角色 + 内置目录”的去重结果，并把类别过滤、未补全状态处理集中实现。

### P1-03  完整存档恢复只有浅层格式校验

证据：engine.ts:131—139 检查部分数组、人数、阶段、座位 ID 去重及少量对象；未逐项验证角色、消息、投票、计时器字段和所有交叉引用。

影响：格式损坏或外部编辑的存档有可能被接纳，随后造成界面异常或不一致状态。建议以统一 schema 严格校验并规范化导入；校验失败必须拒绝创建。恢复仍应保持“新副本、不覆盖原局”的现有原则。

### 发布门槛：权限与联机验收仍然缺席

BUILD_INFO.json 明确记录测试、浏览器检查及云端部署均未执行。package.json 没有 test／typecheck 脚本，build 仅调用 Vite；现有 Pages 流程也没有补上这些验证。应先做类型检查、核心命令测试和跨账号权限验收，再把它称为稳定联机版本。

**来源／范围：** 证据：[S01]、[S03]、[S07]、[S08]；规则核验 [W05]。P1 表示优先修复，不是已经确认发生的安全事件。


---


## 09  一致性、恢复与维护改进

*这些问题不一定阻止第一次启动，但会影响真实开局时的可控性。*

### P1-04  创建／恢复的超时重试没有统一复用请求

Home.tsx:15、17 每次创建或恢复都会重新生成 roomId；Archive 也存在创建副本路径。useRoom 的待确认请求只覆盖游戏命令，成员审批是另一条直接请求。若创建已成功但响应丢失，再次点击可能生成另一个房间。建议建立统一的提交标识、待确认提示与结果恢复；创建超时后先刷新房间列表，避免盲目重复点击。

### P2-01  已知角色的局部夜序覆盖只被保留，未参与运行

scripts.ts:21—23 只有在对象同时具备 name、ability 和合法 team 时，才把它当成完整自定义定义。若导入 {"id":"imp","otherNight":999} 这类已知角色的局部覆盖，数值留在 raw 里，但实际角色仍使用内置夜序。建议明确支持局部覆盖，或显式警告；“导出保留字段”不等于“运行采用字段”。

### P2-02  日夜切换与未结算提名的状态约束不足

engine.ts:98 切阶段时不检查是否还有 open 提名；117—120 的 tally 也没有验证提名是否属于当前白天和轮次。可能出现上一阶段投票遗留，或旧操作在不合适的阶段落地。建议切换前强制结算／取消，服务端 tally 也校验阶段和轮次；需要纠错时使用带原因的独立路径。

### P2-03  备份体积与恢复上限需要统一

存档读取和服务端请求体存在约 4 MB 上限，剧本导入单独限制为 2 MB；而完整存档还包含最近 8 份快照。当前未见导出前的对应体积保证，随着消息和快照增长，存在“能导出，但后续不能按原入口导入”的可能。建议显示大小、约束快照保留、提供不带历史快照的可恢复备份，并验证边界。

### P2-04  轮询、可维护性与发布可重复性

云端页面可见时，即使实时订阅正常也仍每 10 秒轮询。可根据连接状态和是否有更新降低无效读取，但应保留断线恢复。多个 TSX 文件大量逻辑压在单行，增加审核和改错成本；建议统一格式化、拆分表单与命令校验。后端工作流 CLI 使用 latest，也宜固定经过验证的版本。

**来源／范围：** 依据：[S04] useRoom.ts:11—26；[S05] Home.tsx；[S09] Archive.tsx；[S02] scripts.ts；[S03] engine.ts；[S10] 请求体限制；[S12] 工作流。


---


## 10  备份、隐私与常见误区

*先明确备份保存了什么，再考虑把它发给谁、恢复到哪个房间。*

| 对象 | 包含什么／如何处理 |
| --- | --- |
| 完整存档 | 含真实身份、显示身份、说书人标记／笔记、消息、事件及快照。只给需要完整魔典的人保存。 |
| 公开导出 | 按公开视图生成；不要仅凭文件名判断安全，尤其应检查是否已经开放复盘。 |
| 玩家个人笔记 | 云端独立保存在 bt_notes，按作者授权；不是完整 Game 导出的一部分，不能假定会随主持人存档一起备份。 |
| 局面快照 | 阶段切换会留快照，也能手动保存；两类共同使用“最近 8 份”的保留窗口。 |
| 恢复副本 | 产生新的局面 ID／房间码、重置已查看确认和快照；不覆盖原局，也不自动复制原房间的成员绑定。 |

### 建议的实际习惯

发牌前备份配置；重要变更后或结束时导出完整存档；对外只分享核对过的公开版本。恢复联机副本时先确认每个新绑定仍对应原座位本人，因为恢复的座位消息历史也会随局面保留。不要把公开 GitHub 仓库当作秘密魔典的备份盘。

### 为什么同昵称换手机后没有原身份？

昵称不是认证凭据。匿名身份来自当前浏览器会话；换手机通常是新用户。让玩家重新申请，再由说书人明确替换旧设备绑定，而不是试图靠输入相同昵称恢复权限。[W02]

### 为什么发牌后新玩家申请进不来？

发牌会自动锁定房间。新增申请需要先解锁；已经批准的原成员重进，与新用户申请是不同路径。原座位需要换设备时，还要处理旧绑定。

### 为什么夜序或特殊票数没有自动处理？

当前定位是人工主持辅助，不是完整规则解释器。醉毒、角色互动、能力例外、胜负和特殊计票要由说书人判断；导入 jinx 等扩展信息不代表自动执行对应规则。

### 为什么云端旧房间越来越多？

源码限制每个主持人最多 30 个房间，但没有对应的普通用户云端删除入口或自动清理任务。需要补充房间归档／删除产品流程；现阶段管理端清理前务必确认备份、房间 ID 与级联影响。

**来源／范围：** 依据：[S03] 状态／快照／恢复；[S05] 会话与入座；[S09] 导出；[S11] bt_notes、房间限制；[S12] 维护说明。


---


## 11  下一步该做什么

*建议先形成可信的最小闭环，而不是立即扩张成大型游戏平台。*

### 建议的推进顺序

第一阶段，补基础校验：修复旅行者普通提名与自定义入口、完善存档 schema、补统一请求恢复；增加类型检查和核心引擎测试。第二阶段，在独立 Supabase 项目跑通说书人＋多名玩家的完整对局，并做越权和断网验证。第三阶段，再补移动端体验、云端归档删除与发布流程。

### 最低验收清单（本次均未执行）

| 验收项 | 至少应该得到的结果 |
| --- | --- |
| 类型与构建 | 前端／共享引擎类型检查通过；Edge Function 单独检查；干净环境可重建 dist。 |
| 脚本与配板 | 基础、自定义、未知角色均得到明确处理；固定配牌不被随机选项误改。 |
| 发牌一致性 | 多个设备、重复点击、刷新及超时重试后，身份不重抽、不串座；已查看状态对应正确版本。 |
| 权限隔离 | 无登录、待批准、普通玩家、跨房间用户均无法读完整魔典；玩家只能读自己的身份、消息和笔记。 |
| 成员替换 | 旧设备后续访问失效，新设备获得正确座位；不得把曾经送达的数据声称已被远程清除。 |
| 日夜与计票 | 死者票只扣一次；平票、处决不死、旅行者放逐、特殊裁定、跨阶段未结算提名得到正确处理。 |
| 网络与并发 | 双标签页冲突不会覆盖新状态；离线有清楚提示；超时重试不会重复创建或重复改变局面。 |
| 导出与恢复 | 完整备份可往返恢复；公开导出无非预期秘密；接近体积上限时不会出现不可恢复文件。 |
| 浏览器与移动端 | 至少在目标电脑／手机浏览器检查小屏布局、焦点／后台隐藏、登录回调与重新入场。 |

### 哪些能力暂时不宜误认为已经交付

没有内置语音／视频、自由聊天大厅、多人共同说书、全自动角色能力与胜负判定、完整实验角色和相克执行引擎，也没有 PWA 离线联机、社区剧本市场或完整运营后台。这些是范围边界，不能全部当作缺陷；是否新增取决于实际使用需求。

> 总体建议：保留当前“轻量前端＋共享引擎＋Supabase”的方向。先把身份安全、状态一致性、备份恢复和真人主持流程验证可靠，再做体验增强与功能扩展。

**来源／范围：** 建议由第 9—11 页证据推导；原始非目标与验收范围见 [S13]。本页是待执行计划，不是测试报告。


---


## 附录 A  源码证据索引

*所有相对路径均以 ZIP 内的 clocktower/ 为根目录；行号对应本次原文件，不是排版后的说明文档。*

本次已解包读取源文件。ZIP 未返回可检索的正文索引，因此采用“文件路径＋原始行号”定位证据，没有把 README 的自述当作运行结果。压缩后的 TSX 常在同一行包含较长函数，建议在编辑器中按函数名同时查找。

| 编号 | 文件与定位 | 支持的内容 |
| --- | --- | --- |
| S01 | BUILD_INFO.json:1—12<br>package.json:1—31<br>README.md | 构建元数据、未测试／未部署标记、依赖声明、运行命令及交付组成。 |
| S02 | supabase/functions/_shared/catalog.ts:8—135<br>supabase/functions/_shared/scripts.ts:7—38 | 97 个目录条目、三套基础剧本、人数配比；导入限制、未知角色、夜序覆盖与导出。 |
| S03 | supabase/functions/_shared/types.ts:4—49<br>supabase/functions/_shared/engine.ts:14—139 | 角色／座位数据模型、配板、发牌、日夜、投票、投影、快照及恢复。 |
| S04 | src/lib/storage.ts:1—20<br>src/lib/supabase.ts:3—23<br>src/lib/useRoom.ts:9—26 | 本机存储、浏览器配置优先级、身份调用、轮询、版本控制及待确认命令。 |
| S05 | src/features/Home.tsx:9—18<br>src/features/Members.tsx<br>src/features/Player.tsx<br>src/components/AuthPanel.tsx:6—11 | 创建／恢复时的 roomId、匿名加入、审批与换设备、玩家展示隐藏、邮箱及 GitHub 登录。 |
| S06 | src/features/Night.tsx<br>src/features/Messages.tsx<br>engine.ts:22—35、102—107 | 夜间任务、提醒、私密信息与选择请求、黎明公开生死。此处 engine.ts 路径同 S03。 |
| S07 | src/features/Votes.tsx:6—11<br>engine.ts:111—123 | 普通提名与旅行者放逐、投票记录、死者票、票数修正、处决与死亡。 |
| S08 | src/features/Setup.tsx:10—11<br>src/features/SeatInspector.tsx:11<br>engine.ts:81—97 | 配板、旅行者角色选择来源、真实／展示身份、随机或固定发牌。 |

### 关键函数快速查找

createGame：初始局面；applyCommand：游戏命令；publicGame／personalView：不同受众数据；restoreGame：存档副本；importScript：剧本导入；bt_commit_room：原子提交；bt_member_action：座位权限变更。

**来源／范围：** 文件数量与角色数是对上传版本的静态统计。没有检查已打包 dist 与当前源码的逐字节可重现关系，也未验证声明的依赖版本在目标环境一定可安装。


---


## 附录 B  后端、部署与官方核对

*主要结论来自源码；官方资料只用于核对平台和游戏规则，不替代本项目的运行验收。*

| 编号 | 文件与定位 | 支持的内容 |
| --- | --- | --- |
| S09 | src/features/Archive.tsx<br>engine.ts:124—139<br>src/lib/storage.ts:20 | 完整／公开导出、最近快照、恢复新副本、存档读取限制。 |
| S10 | supabase/functions/game/index.ts:5—81<br>supabase/config.toml:1—5 | 服务端密钥、CORS、请求体限制、getUser、成员检查、命令入口与函数设置。 |
| S11 | supabase/schema.sql:8—171 | 9 张 public 表、私有限流表、RLS、显式 grants、投影视图、成员绑定、事务与 Realtime 发布。 |
| S12 | docs/DEPLOYMENT.md<br>.github/workflows/pages.yml<br>.github/workflows/supabase.yml<br>.env.example<br>scripts/serve-dist.mjs | 初始部署顺序、公开／私密变量、两个工作流边界、来源域名、本机监听地址。 |
| S13 | docs/RESEARCH_PLAN.md<br>docs/SCOPE.md<br>docs/CONTENT.md<br>LICENSE | 原始需求、非目标、验收清单与项目自身的内容／授权声明。 |

### 官方核对资料（核对日期：2026-09-10）

[W01] Supabase JavaScript getUser：说明该方法向 Auth 服务验证并获取用户，可传入 JWT；用于核对函数内部认证的含义。

```text
https://supabase.com/docs/reference/javascript/auth-getuser
```

[W02] Supabase Anonymous Sign-ins：匿名用户会话与清除数据／换设备限制。
[W03] Supabase Row Level Security：表权限与行级策略；另检查了平台 changelog 的相关变更。

```text
https://supabase.com/docs/guides/auth/auth-anonymous
https://supabase.com/docs/guides/database/postgres/row-level-security
https://supabase.com/changelog
```

[W04] GitHub Pages Custom Workflows：Pages 构建／部署流程及权限要求。

```text
https://docs.github.com/en/pages/getting-started-with-github-pages/using-custom-workflows-with-github-pages
```

[W05] 官方 Wiki Glossary 与 Undertaker：核对普通处决、旅行者放逐和角色能力例外的区别。

```text
https://wiki.bloodontheclocktower.com/Glossary
https://wiki.bloodontheclocktower.com/Undertaker
```

内容与权利说明：包内声明为非官方辅助工具，MIT 只覆盖项目自有代码，不等于获得游戏商标、美术或规则内容的发行授权。本次未审计原创性与全部第三方权利链；公开发布前仍需核对适用的官方政策和内容来源。[S13]


---
