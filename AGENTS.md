# 项目协作规则

- 开始任务前先考虑能否由 subagent 并行处理独立子任务；适用时使用，完成后不保留无用的运行任务。
- 本项目主要供手机浏览器使用。压缩装饰、重复文案及间距，尽量让主要内容和操作在一屏内可见；保留关键功能，不用裁切或禁止滚动来假装单屏。
- 玩家座位应围绕同一张桌子顺时针排列，保留中央说书人区域。不要把魔典、准备界面、公开城镇改成座位列表或网格。
- 每次修改完成后，先启动或复用本机临时预览供用户测试。交付时提供已验证的 localhost 地址；供手机测试时同时提供本机当前局域网地址，并说明手机需连接同一 Wi-Fi。保持预览服务运行。
- 默认不发布、不部署。用户明确要求发布当前修改后才执行发布；本机预览和生产构建不等于发布授权。
- 本地测试默认使用本机 SQLite，多手机通过同源 /api/local 接口访问。用 npm run dev 同时启动前端与数据库服务；不要只启动 Vite 后交付一个无法联机的预览。未经用户要求不要把本地测试切回 Supabase。启动、端口或存储方式改变时同步更新配置文档。
- UI 变更须检查手机实际渲染、主操作可达性和座位重叠。验证命令来自 package.json：npm run typecheck、npm test、npm run build（完整检查：npm run check）。说明尚未验证的联机或设备场景。
